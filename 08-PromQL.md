
### **PromQL چیست؟**  
**PromQL** یک زبان جستجو (Query Language) است که به شما امکان می‌دهد داده‌های **متریک‌های ذخیره‌شده در Prometheus** را پردازش و تحلیل کنید. با استفاده از **PromQL** می‌توانید داده‌های جمع‌آوری‌شده را **فیلتر، گروه‌بندی و تجزیه و تحلیل** کنید.  

---

## **چگونه PromQL را یاد بگیریم؟**  
برای یادگیری **PromQL**، این مسیر را دنبال کنید:  

### **1️⃣ نصب و راه‌اندازی Prometheus**
🔹 اگر هنوز Prometheus را نصب نکرده‌اید، آن را نصب و راه‌اندازی کنید:  
```sh
docker run -p 9090:9090 prom/prometheus
```
🔹 سپس مرورگر خود را باز کرده و به **http://localhost:9090** بروید.  

---

### **2️⃣ یادگیری دستورات پایه‌ای PromQL**
#### **📌 نمایش مقدار یک متریک ساده**
مثال: نمایش تعداد کل درخواست‌های HTTP:  
```promql
http_requests_total
```
(این دستور تمام مقادیر متریک `http_requests_total` را نمایش می‌دهد.)  

#### **📌 فیلتر کردن داده‌ها بر اساس Label**
اگر متریک دارای **Label** باشد، می‌توان مقدار خاصی را فیلتر کرد:  
```promql
http_requests_total{method="GET", status="200"}
```
(نمایش تعداد درخواست‌های `GET` که پاسخ `200` دریافت کرده‌اند.)  

#### **📌 محاسبات ریاضی روی متریک‌ها**
می‌توان عملیات ریاضی روی متریک‌ها انجام داد. مثلاً تبدیل بایت به مگابایت:  
```promql
memory_usage_bytes / 1024 / 1024
```

---

### **3️⃣ یادگیری توابع مهم در PromQL**
#### **📌 نرخ تغییرات یک متریک (Rate)**
برای مشاهده تعداد درخواست‌ها در هر ثانیه طی 5 دقیقه گذشته:  
```promql
rate(http_requests_total[5m])
```
(این کوئری نرخ تغییرات متریک را در یک بازه‌ی زمانی مشخص نشان می‌دهد.)  

#### **📌 میانگین، ماکزیمم و مینیمم**
- **میانگین مقدار متریک در بین تمام نمونه‌ها:**  
  ```promql
  avg(cpu_usage)
  ```
- **حداکثر مقدار متریک:**  
  ```promql
  max(cpu_usage)
  ```
- **حداقل مقدار متریک:**  
  ```promql
  min(cpu_usage)
  ```

#### **📌 نمایش متریک‌ها در قالب هیستوگرام**
برای مشاهده توزیع زمان پاسخگویی درخواست‌ها:  
```promql
histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m]))
```
(این کوئری صدک **95٪** را برای مدت‌زمان پاسخگویی درخواست‌ها محاسبه می‌کند.)  

---

### **4️⃣ تمرین و آزمایش در Prometheus UI**
🔹 در **Prometheus UI** (آدرس **http://localhost:9090**) می‌توانید کوئری‌های **PromQL** را اجرا کنید و نتایج را به‌صورت **جدول یا نمودار** مشاهده کنید.  

---

### **5️⃣ ترکیب PromQL با Grafana**
برای مشاهده‌ی خروجی کوئری‌های PromQL در یک داشبورد گرافیکی، می‌توانید از **Grafana** استفاده کنید.  
🔹 نصب Grafana با Docker:  
```sh
docker run -d -p 3000:3000 grafana/grafana
```
🔹 سپس در **Grafana**، یک **Data Source** جدید از نوع **Prometheus** اضافه کنید و کوئری‌های **PromQL** را در پنل‌های مختلف اجرا کنید.  

---

### **💡 جمع‌بندی**
✅ برای یادگیری PromQL، باید با **متریک‌ها و لیبل‌ها** در Prometheus آشنا شوید.  
✅ از **کوئری‌های پایه‌ای** شروع کنید و سپس به **توابع پیشرفته مانند Rate و Histogram** بروید.  
✅ برای تمرین، از **Prometheus UI** و **Grafana** استفاده کنید.  

👨‍💻 اگر به یک سناریوی خاص نیاز دارید، بگو تا با هم بررسی کنیم! 😊

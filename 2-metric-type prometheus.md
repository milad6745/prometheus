در **Prometheus**، متریک‌ها (Metrics) به **چهار نوع اصلی** تقسیم می‌شوند که هرکدام برای کاربرد خاصی طراحی شده‌اند. این چهار نوع عبارت‌اند از:  

### **1️⃣ Counter (شمارنده)**
**ویژگی:** مقدار آن فقط می‌تواند افزایش یابد (یا ریست شود).  
**کاربرد:** شمارش تعداد رخدادها، مانند تعداد درخواست‌های HTTP، تعداد خطاها، تعداد پردازش‌های تکمیل‌شده.  
🔹 **مثال:**  
```promql
http_requests_total
```
(تعداد کل درخواست‌های HTTP از زمان شروع سرویس)  

---

### **2️⃣ Gauge (مقدار متغیر)**
**ویژگی:** مقدار آن می‌تواند افزایش یا کاهش یابد.  
**کاربرد:** نشان دادن مقادیر لحظه‌ای مانند دمای CPU، میزان حافظه استفاده‌شده، تعداد کانکشن‌های فعال.  
🔹 **مثال:**  
```promql
memory_usage_bytes
```
(میزان حافظه استفاده‌شده در بایت)  

---

### **3️⃣ Histogram (هیستوگرام)**
**ویژگی:** مقدار یک متریک را در بازه‌های مشخصی (buckets) تقسیم‌بندی می‌کند و تعداد مشاهدات را در هر بازه نگه می‌دارد.  
**کاربرد:** بررسی زمان پاسخ درخواست‌ها، توزیع اندازه درخواست‌ها.  
🔹 **مثال:**  
```promql
http_request_duration_seconds_bucket
```
(توزیع زمان پاسخ درخواست‌های HTTP در بازه‌های مشخص)  

🔸 این متریک شامل سه مقدار کلیدی است:  
- `_bucket` ➖ تعداد مشاهدات در هر بازه  
- `_sum` ➖ مجموع کل مقادیر مشاهده‌شده  
- `_count` ➖ تعداد کل مشاهدات  

---

### **4️⃣ Summary (خلاصه)**
**ویژگی:** مشابه Histogram است، اما درصدهای مشخصی (Quantiles) را نیز محاسبه می‌کند.  
**کاربرد:** بررسی تأخیر در پاسخگویی و توزیع داده‌ها با نمایش مقادیر مشخص مانند **میانه (p50)، صدک ۹۰ (p90) و صدک ۹۹ (p99)**.  
🔹 **مثال:**  
```promql
http_request_duration_seconds{quantile="0.99"}
```
(مدت‌زمان پاسخگویی درخواست HTTP در **صدک ۹۹**)  

---

### **مقایسه Histogram و Summary**
| ویژگی  | **Histogram** | **Summary** |
|--------|-------------|-------------|
| **بازه‌های از پیش تعریف‌شده** | ✅ دارد | ❌ ندارد |
| **محاسبه Quantile در سمت کلاینت** | ❌ خیر | ✅ بله |
| **محاسبه Quantile در سمت سرور** | ✅ بله | ❌ خیر |
| **جمع‌شدن مقادیر در چندین نمونه Prometheus** | ✅ بله | ❌ خیر |

🔹 اگر نیاز دارید داده‌ها را در چندین **Prometheus** ادغام کنید، از **Histogram** استفاده کنید.  
🔹 اگر فقط به **Quantile دقیق در همان سرویس** نیاز دارید، **Summary** بهتر است.  

---

### **جمع‌بندی کلی**
✅ **Counter** برای شمارش رویدادهایی که فقط افزایش می‌یابند.  
✅ **Gauge** برای مقادیر متغیری که بالا و پایین می‌روند.  
✅ **Histogram** برای توزیع داده‌ها در بازه‌های مشخص.  
✅ **Summary** برای محاسبه درصدهای خاص (Quantiles).  

کدام نوع متریک را می‌خواهید در پروژه‌تان استفاده کنید؟ 😊

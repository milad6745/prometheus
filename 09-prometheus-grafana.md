بسیار عالی! استفاده از **Prometheus** و **Grafana** برای مانیتورینگ داکر یک انتخاب قدرتمند و مقیاس‌پذیر است. در اینجا یک راهنمای کلی برای نصب و پیکربندی این دو ابزار برای نظارت بر کانتینرهای داکر آورده شده:

### 1. **نصب Prometheus**

ابتدا باید **Prometheus** را روی یکی از ماشین‌ها یا سرورها نصب کنید.

#### الف) نصب Prometheus با استفاده از Docker:
1. یک کانتینر برای Prometheus راه‌اندازی کنید:
   ```bash
   docker run -d \
   --name=prometheus \
   -p 9090:9090 \
   -v /path/to/prometheus.yml:/etc/prometheus/prometheus.yml \
   prom/prometheus
   ```

   توجه داشته باشید که در دستور بالا فایل `prometheus.yml` باید تنظیمات مربوط به کانفیگ Prometheus رو داشته باشه (در مرحله بعد توضیح می‌دم).

#### ب) فایل پیکربندی Prometheus (`prometheus.yml`):
در مسیر `/path/to/prometheus.yml` باید تنظیمات Prometheus رو وارد کنید. برای مانیتورینگ داکر معمولاً به این شکل تنظیم می‌شود:

```yaml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'docker'
    static_configs:
      - targets: ['localhost:9323'] # Prometheus این پورت رو برای دریافت داده‌ها از cAdvisor استفاده می‌کنه
```

### 2. **نصب cAdvisor**

برای اینکه Prometheus بتونه داده‌های مربوط به کانتینرهای داکر رو جمع‌آوری کنه، از **cAdvisor** استفاده می‌کنیم.

#### الف) راه‌اندازی cAdvisor با Docker:
1. cAdvisor رو به‌عنوان یک کانتینر داکر اجرا کنید:
   ```bash
   docker run -d \
   --name=cadvisor \
   --privileged \
   -p 8080:8080 \
   -p 9323:9323 \
   google/cadvisor:latest
   ```

2. حالا cAdvisor داده‌های مربوط به کانتینرهای داکر رو در پورت `9323` در اختیار Prometheus قرار می‌ده.

### 3. **نصب Grafana**

Grafana برای نمایش داده‌ها و داشبوردهای گرافیکی استفاده می‌شود.

#### الف) راه‌اندازی Grafana با Docker:
1. Grafana رو به‌عنوان کانتینر اجرا کنید:
   ```bash
   docker run -d \
   --name=grafana \
   -p 3000:3000 \
   grafana/grafana
   ```

2. برای ورود به Grafana به `http://<server-ip>:3000` برید و با یوزر `admin` و پسورد `admin` وارد شوید (اولین ورود معمولاً این است، ولی پسورد رو بعد از ورود می‌تونید تغییر بدید).

### 4. **اتصال Grafana به Prometheus**

بعد از اینکه Grafana رو راه‌اندازی کردید، باید اون رو به Prometheus متصل کنید تا داده‌ها رو از اون دریافت کنه.

1. وارد داشبورد Grafana بشید و به بخش **Configuration** > **Data Sources** برید.
2. گزینه **Prometheus** رو انتخاب کنید.
3. آدرس Prometheus رو وارد کنید (اگر روی همون ماشین اجرا می‌کنید، آدرس `http://localhost:9090` خواهد بود).
4. روی **Save & Test** کلیک کنید تا اتصال برقرار بشه.

### 5. **ساخت داشبورد در Grafana**

حالا که Prometheus و Grafana متصل شدند، می‌تونید یک داشبورد برای مشاهده داده‌ها بسازید.

1. وارد داشبورد Grafana بشید و روی **Create** > **Dashboard** کلیک کنید.
2. در بخش **Add Query**، منبع داده رو به **Prometheus** تغییر بدید.
3. می‌تونید کوئری‌های Prometheus رو برای مشاهده داده‌های داکر بنویسید. برای مثال، برای مشاهده استفاده از CPU:
   ```prometheus
   rate(container_cpu_usage_seconds_total{job="docker"}[1m])
   ```
4. با توجه به داده‌های موجود، چندین گراف می‌تونید بسازید (مثل مصرف CPU، حافظه، استفاده از دیسک و غیره).

### 6. **استفاده از داشبوردهای آماده**

Grafana داشبوردهای آماده‌ای برای مانیتورینگ داکر هم داره که می‌تونید از اون‌ها استفاده کنید:

1. وارد Grafana بشید و از منوی سمت چپ روی **Explore** کلیک کنید.
2. در بخش **Dashboards**، سرچ کنید برای "docker".
3. یکی از داشبوردهای آماده رو انتخاب کنید و اون رو به داشبورد خودتون اضافه کنید.

این داشبوردهای آماده معمولاً شامل گراف‌هایی برای نمایش میزان استفاده از منابع، تعداد کانتینرها، وضعیت سلامت کانتینرها و دیگر داده‌های مرتبط با داکر هستند.

### نتیجه‌گیری

حالا شما می‌تونید با استفاده از **Prometheus** داده‌ها رو جمع‌آوری کنید و با **Grafana** به‌صورت گرافیکی این داده‌ها رو مانیتور کنید. این ترکیب به شما این امکان رو می‌ده تا به‌صورت دقیق و مقیاس‌پذیر وضعیت کانتینرهای داکر رو زیر نظر بگیرید.

اگر نیاز به جزئیات بیشتری دارید یا در بخش خاصی از فرآیند دچار مشکل شدید، خوشحال می‌شم کمک کنم!

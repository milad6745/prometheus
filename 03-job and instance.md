در Prometheus، دو مفهوم مهم وجود دارد: **Jobs** و **Instances**. این دو برای سازماندهی و شناسایی منابعی که Prometheus از آن‌ها داده جمع‌آوری می‌کند، به‌کار می‌روند. در ادامه توضیح دقیق و ساده‌ای از هرکدام آورده‌ام:

---

### ✅ 1. **Job**

* یک **Job** مجموعه‌ای از هدف‌ها (targets) است که کار مشابهی انجام می‌دهند یا در یک دسته قرار می‌گیرند.
* به‌عنوان مثال: یک Job می‌تواند "web\_servers" باشد که شامل چندین سرور وب است.
* این نام در فایل پیکربندی Prometheus (`prometheus.yml`) مشخص می‌شود.

#### مثال:

```yaml
scrape_configs:
  - job_name: 'web_servers'
    static_configs:
      - targets: ['192.168.1.10:9100', '192.168.1.11:9100']
```

---

### ✅ 2. **Instance**

* یک **Instance** یک هدف (target) خاص است که Prometheus از آن داده جمع‌آوری می‌کند.
* به‌طور دقیق‌تر، Instance برابر است با ترکیب IP (یا hostname) و پورت یک سرویس مانیتورینگ شده.
* در مثال بالا، `192.168.1.10:9100` یک instance است.

---

### 🔁 رابطه بین Job و Instance:

* یک Job شامل چندین Instance است.
* هر Instance معمولاً یک سرور یا اپلیکیشن با آدرس خاص است.
* Prometheus داده‌ها را برای هر instance به‌طور جداگانه ذخیره می‌کند، همراه با لیبل‌های `job` و `instance`.

---

### 🧠 در عمل:

اگر در Prometheus کوئری مثل زیر بنویسی:

```promql
up
```

خروجی شامل لیبل‌هایی مثل زیر خواهد بود:

| instance          | job          | value |
| ----------------- | ------------ | ----- |
| 192.168.1.10:9100 | web\_servers | 1     |
| 192.168.1.11:9100 | web\_servers | 1     |

---

اگر خواستی، می‌تونم یک مثال کامل‌تر از فایل پیکربندی یا نحوه کوئری زدن در Prometheus هم برات بنویسم.

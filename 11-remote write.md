سلام!
قابلیت **Prometheus Remote Write** یکی از ویژگی‌های Prometheus است که امکان ارسال داده‌های جمع‌آوری‌شده (metrics) را به یک سیستم خارجی (مانند یک دیتابیس سری زمانی دیگر یا سیستم مانیتورینگ ابری) فراهم می‌کند.

---

### 📌 تعریف ساده:

`remote_write` قابلیتی در Prometheus است که **داده‌ها را به‌صورت مداوم و real-time** به یک مقصد دیگر (مانند Cortex، Thanos، InfluxDB، VictoriaMetrics و ...) می‌فرستد.

---

### 🎯 کاربردها:

* ذخیره طولانی‌مدت داده‌ها (Long-term storage)
* استفاده از سیستم‌های توزیع‌شده و مقیاس‌پذیرتر
* تجزیه و تحلیل پیشرفته‌تر با ابزارهای دیگر
* اشتراک‌گذاری داده‌ها با تیم‌ها یا سیستم‌های دیگر

---

### 🛠️ نحوه پیکربندی:

در فایل تنظیمات `prometheus.yml` این بخش را اضافه می‌کنیم:

```yaml
remote_write:
  - url: "http://remote-storage:9201/write"
    basic_auth:
      username: "your_username"
      password: "your_password"
    write_relabel_configs:
      - source_labels: [__name__]
        regex: ".*"
        action: keep
```

---

### 🧩 نکات مهم:

* داده‌ها به‌صورت HTTP و با فرمت protobuf ارسال می‌شوند.
* عملکرد Prometheus را کمی تحت تأثیر قرار می‌دهد (چون ارسال داده همزمان با جمع‌آوری انجام می‌شود).
* می‌توانید با `write_relabel_configs` مشخص کنید چه داده‌هایی ارسال شوند.

---

### 🆚 تفاوت با Remote Read:

| ویژگی      | Remote Write                | Remote Read                         |
| ---------- | --------------------------- | ----------------------------------- |
| هدف        | ارسال داده به سیستم دیگر    | خواندن داده از منبع خارجی           |
| جهت انتقال | خروجی (outbound)            | ورودی (inbound)                     |
| کاربرد     | آرشیو، مانیتورینگ توزیع‌شده | Query ترکیبی، تجزیه‌ و تحلیل تاریخی |

---

اگر مایل باشید می‌تونم چند مقصد رایج برای `remote_write` یا حتی یک نمونه واقعی از اتصال به **VictoriaMetrics** یا **Grafana Cloud** براتون بنویسم.

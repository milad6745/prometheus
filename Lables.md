در **Prometheus**، برچسب‌ها (**labels**) یکی از ویژگی‌های کلیدی متریک‌ها هستند که به شما این امکان را می‌دهند که داده‌ها را با ابعاد مختلف فیلتر و گروه‌بندی کنید.  

### 🔹 مفهوم Label در Prometheus
هر متریک در Prometheus دارای **نام** (metric name) و **مجموعه‌ای از برچسب‌ها (labels)** است. برچسب‌ها به شما کمک می‌کنند که متریک‌ها را **طبقه‌بندی** کنید و از طریق **پرس‌وجوها (queries)** داده‌های خاص را استخراج کنید.

### 📌 مثال ساده
فرض کنید متریکی دارید که تعداد درخواست‌های دریافتی یک سرویس را شمارش می‌کند:
```plaintext
http_requests_total
```
بدون برچسب، این متریک فقط تعداد کلی درخواست‌ها را نشان می‌دهد، اما اگر بخواهید تعداد درخواست‌ها را **بر اساس مسیر (path) و روش (method)** تفکیک کنید، می‌توانید از **labels** استفاده کنید:

```plaintext
http_requests_total{method="GET", path="/home"}
http_requests_total{method="POST", path="/login"}
http_requests_total{method="GET", path="/dashboard"}
```

در اینجا، متریک **http_requests_total** دو برچسب دارد:
- **method** (روش HTTP، مثلاً GET یا POST)
- **path** (مسیر درخواست، مثل `/home` یا `/login`)

### 🔍 استفاده در PromQL
با استفاده از این برچسب‌ها، می‌توانیم کوئری‌های دقیق‌تری بنویسیم. مثلاً:
- تعداد کل درخواست‌های `GET`:
  ```promql
  http_requests_total{method="GET"}
  ```
- تعداد درخواست‌های `/login`:
  ```promql
  http_requests_total{path="/login"}
  ```
- مجموع درخواست‌های همه مسیرها:
  ```promql
  sum(http_requests_total)
  ```
  
### 🎯 مزایای استفاده از Labels
✅ فیلتر کردن و گروه‌بندی متریک‌ها  
✅ ایجاد **Dashboardهای دقیق‌تر** در **Grafana**  
✅ تحلیل بهتر عملکرد سرویس‌ها  
✅ امکان مشاهده روند تغییرات داده‌ها در ابعاد مختلف  

### 🚀 نکته مهم: Label زیاد نزنید!  
استفاده بیش از حد از **برچسب‌ها** ممکن است باعث **افزایش حجم داده‌ها (high cardinality)** و کاهش کارایی **Prometheus** شود. پس فقط زمانی از برچسب استفاده کنید که واقعاً نیاز است.  

**اگر پرسش خاصی در مورد برچسب‌ها داری، بگو تا دقیق‌تر توضیح بدم! 😊**
